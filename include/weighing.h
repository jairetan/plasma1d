//General weighing scheme
#include <vector>
#include "particle.h"
#pragma once

void weighing (Particle *particle, double *weights);
void alt_weighing (Particle *particle, double *weights);
void zero_order_weighing (Particle *particle, double *weights);
