#include "mode_diagnostic.h"

//Time history of chosen Fourier mdoes
void mode_diagnostic (std::vector <double> *transformed_field, double t)
{
    //std::ofstream mode_out ("mode_"+ to_string (mode)+"_out.dat", std::ios::app) transformed_field->at (0);

    //Add first ~20 modes together
    //To get power spectrum, take fourier transform again
    //Find dispersion relation plot
    //std::ofstream mode_out ("mode_"+ to_string (mode)+"_out.dat", std::ios::app);
    //mode_out << t << " " << val;
    //mode_out.close();
}
